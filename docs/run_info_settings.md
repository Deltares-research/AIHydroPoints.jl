# Run info settings

The `[run_info]` table identifies a training run and is only required for `train.jl`.

| Field | Description |
|---|---|
| `runid` | Unique identifier for the run; used to name the output directory and leaderboard entry. |
| `description` | Free-text description saved alongside the model for reference. |
